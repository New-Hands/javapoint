package thread.threadpool;

import org.apache.commons.lang3.concurrent.BasicThreadFactory;

import java.util.concurrent.*;

/**
 * <P>简单的线程池使用示例</P>
 *
 * @author lst
 */

public class ThreadPool {

    public static final int INSTANT = 10;
    //public static final Executor EXECUTOR = Executors.newFixedThreadPool(INSTANT);

    public static void main(String[] args) {
       /* ThreadPoolExecutor poolExecutor1 = new ThreadPoolExecutor(10,20,(1000*60*5L),TimeUnit.SECONDS,);
        //BasicThreadFactory.Builder
    //ThreadFactory threadFactory = Thread
    ScheduledThreadPoolExecutor  scheduledThreadPoolExecutor = new ScheduledThreadPoolExecutor();*/
      //  ThreadFactory factory =

    }
}
