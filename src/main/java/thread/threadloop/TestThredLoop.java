package thread.threadloop;

/**
 * <p> 线程死锁的原因 互相持有对方所需仅有资源，谁都不释放 </p>
 * <p>避免线程死锁 1.在获取不同的资源时，确定一种获取顺序，避免互相持有。 2.采用同时获取多个资源的方式</p>
 * <p>就餐问题的典型案列</p>
 * @author 李尚庭
 * @date
 */
public class TestThredLoop {

}
